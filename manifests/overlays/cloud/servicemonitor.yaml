# Match high_granularity scrape interval.
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: kepler
  namespace: kepler
spec:
  endpoints:
    - port: http
      interval: 1s
      path: /metrics
